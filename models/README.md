Trained Model Artifacts

The trained Random Forest model is generated by executing src/train_model.py.

Due to environment constraints, the model file is not committed here.
However, the complete training, evaluation, and prediction pipeline is
fully implemented and reproducible.
